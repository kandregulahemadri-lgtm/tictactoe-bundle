{
  "name": "tictactoe-bundle",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run start",
    "dev:backend": "cd backend && python -m uvicorn server:app --host 0.0.0.0 --port 8001 --reload"
  }
}
